# Application Configuration
# Copy this file to .env and fill in your actual values

# Supabase Configuration (Required)
# Get these from your Supabase project dashboard
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Legacy React App Environment Variables (if needed for compatibility)
REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Stripe Configuration (SaaS Mode)
# Client-side publishable key (safe to expose)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# Server-side secret key (Supabase Edge Functions only)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
# Webhook signing secret (from Stripe Dashboard)
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-signing-secret

# Build Mode Configuration
REACT_APP_BUILD_MODE=saas

# AI Configuration (Server-side only)
# Google Gemini API key is now configured server-side in Supabase Edge Functions
# No client-side API keys needed for AI features

# Error Tracking & Monitoring (Production)
# Get DSN from your Sentry project dashboard
REACT_APP_SENTRY_DSN=https://your-sentry-dsn@your-project.ingest.sentry.io/your-project-id
REACT_APP_VERSION=1.0.0

# Application Settings (Optional)
NODE_ENV=development
LOG_LEVEL=warn

# Security Note:
# NEVER commit your actual .env file to version control
# These environment variables are now REQUIRED - the app will not start without them
# For local development, copy this file to .env and fill in your actual Supabase credentials
# Stripe webhook URL will be: https://your-app.northflank.app/api/stripe-webhook